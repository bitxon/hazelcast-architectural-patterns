apiVersion: v1
kind: Service
metadata:
  name: app-hz-sidecar
  labels:
    app: app-hz-sidecar
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: app-hz-sidecar
